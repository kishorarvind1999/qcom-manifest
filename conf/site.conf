# This configuration file is dynamically generated every time
# set_bb_env.sh is sourced to set up a workspace.  DO NOT EDIT.
#--------------------------------------------------------------
SCONF_VERSION = "1"

# Where to store sources
DL_DIR = "/home/kishor/Projects/qim_manifest/downloads"

# Where to save shared state
SSTATE_DIR = "/home/kishor/Projects/qim_manifest/sstate-cache"

# Add codelinaro sites to MIRRORS
MIRRORS += "git://github.com git://git.codelinaro.org/clo/yocto-mirrors/github/ git://.*/.*/ git://git.codelinaro.org/clo/yocto-mirrors/ https://.*/.*/ https://codelinaro.jfrog.io/artifactory/codelinaro-le/ "

